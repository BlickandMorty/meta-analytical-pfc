# Pythia checkpoint drift study
# Fill revisions with valid checkpoint tags for the model.

study:
  prompts_path: "data/prompts.txt"

checkpoints:
  - model_name: "EleutherAI/pythia-70m"
    device: "cuda"
    dtype: "float16"
    load_in_4bit: false
    revisions:
      # Example entries (update with real checkpoint tags)
      # - name: "step000100"
      #   step: 100
      # - name: "step000200"
      #   step: 200
      []
