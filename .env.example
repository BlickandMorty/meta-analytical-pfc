# Meta-Analytical PFC — Environment Variables
# Copy to .env and fill in your values:  cp .env.example .env

# Anthropic API (required for hybrid mode)
# Get your key at https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-your-api-key-here

# Inference mode: "local" (no API key needed) or "hybrid" (uses Anthropic API)
PFC_INFERENCE_MODE=local

# Logging: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
